# タスクの状態

> [!summary] 一句話
> タスク在実行可能、実行、待機三種狀態間，因 CPU 分配與輸入輸出事件而轉換。

Task 主要會在三種狀態之間轉換。

## 実行可能状態
- 已經準備好執行
- 正在等待 CPU

## 実行状態
- 正在使用 CPU 執行

## 待機状態
- 因為等待輸入輸出等事件，暫時不能執行
- 例：等待磁碟讀取、鍵盤輸入、印表機完成

## 狀態轉換
Task 生成
↓
実行可能状態
↓ CPU 分配
実行状態
↓ 等待輸入輸出
待機状態
↓ 等待完成
実行可能状態

実行状態
↓ 執行完成
Task 消滅
